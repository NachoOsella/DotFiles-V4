[[mgr.prepend_keymap]]
on  = "y"
run = [ 'shell -- for path in %s; do echo "file://$path"; done | wl-copy -t text/uri-list', "yank" ]

[[mgr.prepend_keymap]]
on  = "<C-t>"
run = "shell --orphan -- kitty --single-instance"
desc = "Abrir Kitty en el CWD"
